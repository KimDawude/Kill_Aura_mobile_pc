local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UIS = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer

-- [[ PERFORMANCE CACHE ]]
local Vector3New = Vector3.new
local taskSpawn = task.spawn

-- // SETTINGS
local Settings = {
    Enabled = false,
    Range = 25,
    RangeSq = 625, -- 25^2 (Pre-calculated for speed)
    Target = nil, 
    SearchTarget = nil 
}

-- Remote Path
local HitRemote = nil
pcall(function()
    HitRemote = game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("Knit"):WaitForChild("Services"):WaitForChild("CombatService"):WaitForChild("RF"):WaitForChild("Hit")
end)

-- ==================== UI CONSTRUCTION ====================
local ScreenGui = Instance.new("ScreenGui", game:GetService("CoreGui"))
ScreenGui.Name = "KRISPhub_V15"
ScreenGui.ResetOnSpawn = false

local ToggleButton = Instance.new("TextButton", ScreenGui)
ToggleButton.Size = UDim2.new(0, 100, 0, 40)
ToggleButton.Position = UDim2.new(0, 20, 0.15, 0)
ToggleButton.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
ToggleButton.Text = "KRISPhub"
ToggleButton.TextColor3 = Color3.new(1, 1, 1)
ToggleButton.Font = Enum.Font.GothamBold
Instance.new("UICorner", ToggleButton)
Instance.new("UIStroke", ToggleButton).Color = Color3.new(0.4, 0.4, 1)

local Main = Instance.new("Frame", ScreenGui)
Main.Size = UDim2.new(0, 350, 0, 250)
Main.Position = UDim2.new(0.5, -175, 0.5, -125)
Main.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
Main.Visible = false
Main.Active = true
Instance.new("UICorner", Main)

local SearchBar = Instance.new("TextBox", Main)
SearchBar.Size = UDim2.new(0, 140, 0, 30)
SearchBar.Position = UDim2.new(0.05, 0, 0.5, 0)
SearchBar.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
SearchBar.PlaceholderText = "Type Name..."
SearchBar.Text = ""
SearchBar.TextColor3 = Color3.new(1, 1, 1)
SearchBar.Font = Enum.Font.Gotham
SearchBar.TextSize = 12
Instance.new("UICorner", SearchBar)

local EnableBtn = Instance.new("TextButton", Main)
EnableBtn.Size = UDim2.new(0, 140, 0, 45)
EnableBtn.Position = UDim2.new(0.05, 0, 0.1, 0)
EnableBtn.Text = "DISABLED"
EnableBtn.Font = Enum.Font.GothamBold
EnableBtn.TextColor3 = Color3.new(1, 0.3, 0.3)
EnableBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
Instance.new("UICorner", EnableBtn)

local StatusLabel = Instance.new("TextLabel", Main)
StatusLabel.Size = UDim2.new(0, 140, 0, 20)
StatusLabel.Position = UDim2.new(0.05, 0, 0.35, 0)
StatusLabel.Text = "MODE: CLOSEST"
StatusLabel.TextColor3 = Color3.new(1, 1, 1)
StatusLabel.BackgroundTransparency = 1
StatusLabel.Font = Enum.Font.GothamBold
StatusLabel.TextSize = 10

local Scroll = Instance.new("ScrollingFrame", Main)
Scroll.Size = UDim2.new(0.48, 0, 0.85, 0)
Scroll.Position = UDim2.new(0.48, 0, 0.07, 0)
Scroll.BackgroundTransparency = 1
Scroll.ScrollBarThickness = 2
local ListLayout = Instance.new("UIListLayout", Scroll)
ListLayout.Padding = UDim.new(0, 4)

-- ==================== LOGIC ====================

local function UpdateList()
    for _, v in pairs(Scroll:GetChildren()) do if v:IsA("TextButton") then v:Destroy() end end
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer then
            local b = Instance.new("TextButton", Scroll)
            b.Size = UDim2.new(0.9, 0, 0, 30)
            b.BackgroundColor3 = (Settings.Target == p) and Color3.fromRGB(0, 100, 200) or Color3.fromRGB(25, 25, 30)
            b.Text = p.DisplayName
            b.TextColor3 = Color3.new(1, 1, 1)
            b.Font = Enum.Font.Gotham
            Instance.new("UICorner", b)
            
            b.MouseButton1Click:Connect(function()
                if Settings.Target == p then
                    Settings.Target = nil
                    StatusLabel.Text = "MODE: CLOSEST"
                else
                    Settings.Target = p
                    StatusLabel.Text = "TARGET: " .. p.DisplayName:upper()
                    SearchBar.Text = "" 
                end
                UpdateList()
            end)
        end
    end
end

SearchBar:GetPropertyChangedSignal("Text"):Connect(function()
    local text = SearchBar.Text:lower()
    if text == "" then
        Settings.SearchTarget = nil
        if not Settings.Target then StatusLabel.Text = "MODE: CLOSEST" end
        return
    end
    
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and (p.Name:lower():find(text) or p.DisplayName:lower():find(text)) then
            Settings.SearchTarget = p
            Settings.Target = nil 
            StatusLabel.Text = "SEARCH: " .. p.DisplayName:upper()
            return
        end
    end
end)

-- Draggable Logic
local function Drag(obj)
    local dragging, startPos, dragStart
    obj.InputBegan:Connect(function(i) 
        if i.UserInputType == Enum.UserInputType.Touch or i.UserInputType == Enum.UserInputType.MouseButton1 then 
            dragging = true; dragStart = i.Position; startPos = obj.Position 
        end 
    end)
    UIS.InputChanged:Connect(function(i) 
        if dragging and (i.UserInputType == Enum.UserInputType.Touch or i.UserInputType == Enum.UserInputType.MouseMovement) then 
            local delta = i.Position - dragStart
            obj.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y) 
        end 
    end)
    UIS.InputEnded:Connect(function() dragging = false end)
end

Drag(Main); Drag(ToggleButton)

ToggleButton.MouseButton1Click:Connect(function() Main.Visible = not Main.Visible end)

EnableBtn.MouseButton1Click:Connect(function()
    Settings.Enabled = not Settings.Enabled
    EnableBtn.Text = Settings.Enabled and "RUNNING" or "DISABLED"
    EnableBtn.TextColor3 = Settings.Enabled and Color3.new(0.3, 1, 0.3) or Color3.new(1, 0.3, 0.3)
    EnableBtn.BackgroundColor3 = Settings.Enabled and Color3.fromRGB(30, 50, 30) or Color3.fromRGB(30, 30, 40)
end)

-- ==================== HIGH-SPEED ENGINE = [RenderStepped] ====================

local function GetClosest()
    local closestPlr = nil
    local shortestDistSq = Settings.RangeSq
    local myHrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    
    if not myHrp then return nil end
    local myPos = myHrp.Position
    
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character then
            local hrp = p.Character:FindFirstChild("HumanoidRootPart")
            local hum = p.Character:FindFirstChild("Humanoid")
            if hrp and hum and hum.Health > 0 then
                local pPos = hrp.Position
                local dx, dy, dz = pPos.X - myPos.X, pPos.Y - myPos.Y, pPos.Z - myPos.Z
                local distSq = (dx*dx + dy*dy + dz*dz)
                if distSq < shortestDistSq then
                    shortestDistSq = distSq
                    closestPlr = p
                end
            end
        end
    end
    return closestPlr
end

RunService.RenderStepped:Connect(function()
    if not Settings.Enabled or not HitRemote then return end
    
    local activeTarget = Settings.SearchTarget or Settings.Target or GetClosest()
    
    if activeTarget and activeTarget.Character then
        local hum = activeTarget.Character:FindFirstChild("Humanoid")
        local hrp = activeTarget.Character:FindFirstChild("HumanoidRootPart")
        local myChar = LocalPlayer.Character
        
        if hum and hum.Health > 0 and hrp and myChar then
            local myHrp = myChar:FindFirstChild("HumanoidRootPart")
            if myHrp then
                local myPos = myHrp.Position
                local pPos = hrp.Position
                local dx, dy, dz = pPos.X - myPos.X, pPos.Y - myPos.Y, pPos.Z - myPos.Z
                
                if (dx*dx + dy*dy + dz*dz) <= Settings.RangeSq then
                    -- High-Frequency Fire
                    taskSpawn(function()
                        pcall(HitRemote.InvokeServer, HitRemote, hum, pPos)
                    end)
                end
            end
        end
    end
end)

UpdateList()
Players.PlayerAdded:Connect(UpdateList)
Players.PlayerRemoving:Connect(UpdateList)
